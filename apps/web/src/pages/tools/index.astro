---
import Layout from '../../layouts/Layout.astro';
import ToolCards from '../../components/ToolCards.astro';
---
<Layout title="MCP Tool Catalog" description="Site Audit combines DNS, TLS, HTTP, redirects, and security headers for 5 sats. Explore all four InvokeWorks tools and their per-call prices.">
  <section class="section"><div class="eyebrow">Catalog · Transparent per-call pricing</div><h1>Tools that earn their call.</h1><p class="lead">Start with Site Audit for one combined report. Choose DNS Lookup, HTTP Inspect, or TLS Inspect for a focused check. LiveAuth handles authentication and metering for all four.</p><ToolCards /></section>
</Layout>
